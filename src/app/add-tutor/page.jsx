import { AddTutorClient } from "@/components/add-tutor/AddTutorClient";

export const metadata = {
  title: "Add New Tutor",
};

const AddTutorPage = () => {
  return <AddTutorClient />;
};

export default AddTutorPage;
